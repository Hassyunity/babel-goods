{
  "name": "babel-monorepo",
  "private": true,
  "scripts": {
    "start:backend": "cd backend && bundle install && bundle exec rails s -p 3000 -b 0.0.0.0",
    "start:frontend": "cd frontend && npm install && npm run dev",
    "dev": "concurrently -k \"npm:dev:backend\" \"npm:dev:frontend\"",
    "dev:backend": "cd backend && bundle install && bundle exec rails s -p 3000 -b 0.0.0.0",
    "dev:frontend": "cd frontend && npm install && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
